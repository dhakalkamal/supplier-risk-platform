# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Virtual environments — never copy these into the image
.venv/
venv/
env/

# Local config — secrets must come from env vars, not baked into the image
.env
*.env

# Frontend — built separately in its own stage
frontend/

# Dev tooling
.git/
.gitignore
tests/
docs/
prompts/
infra/
*.md

# Data pipeline artefacts (not needed at runtime)
data/
ml/
